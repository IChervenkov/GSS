<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="theme-color" content="#1d4ed8" />
  <title><%= title %></title>
  <link rel="icon" href="/assets/shared/icon/logo-page.png">
  <link rel="stylesheet" href="/assets/shared/css/core/auth-base.css" />
  <% const resolvedPageStyles = typeof pageStyles !== 'undefined' ? pageStyles : []; %>
  <% resolvedPageStyles.forEach((href) => { %>
  <link rel="stylesheet" href="<%= href %>" />
  <% }) %>
</head>