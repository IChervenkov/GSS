<div id="<%= id %>" class="alert alert--danger <%= isVisible ? 'is-visible' : '' %>" role="alert" aria-live="polite">
  <span class="alert__dot" aria-hidden="true"></span>
  <span id="<%= textId %>" class="alert__text"><%= message || '' %></span>
</div>