name: bike_app
description: "GSS Bike mobile client for bicycle fleet operations."
publish_to: 'none'
version: 1.4.1+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_messaging: ^16.0.4
  flutter_secure_storage: ^10.0.0
  http: ^1.6.0
  socket_io_client: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
